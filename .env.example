# Environment Variables for Local Kafka + Vault Demo
# Copy this file to .env and update with your values
#
# ⚠️  SECURITY WARNING: Never commit .env to git! It's already in .gitignore

# Vault Configuration
# The address of the Vault server (local Docker instance)
VAULT_ADDR=http://localhost:8200

# Vault root token for authentication
# For local dev mode, Docker Compose uses 'dev-only-token' by default
# For production, never use dev tokens in logs or version control
VAULT_TOKEN=dev-only-token

# Vault namespace (for Vault Enterprise)
# For Vault Enterprise with namespaces, specify the namespace here
# For open source Vault or local dev, set to empty string
VAULT_NAMESPACE=

# Vault image (optional)
# Defaults to Vault OSS. Use Enterprise image for Transform secrets engine (FPE)
# VAULT_IMAGE=hashicorp/vault-enterprise:1.17.0-ent

# Optional: Custom Docker Compose project name
# COMPOSE_PROJECT_NAME=kafka-vault-demo
